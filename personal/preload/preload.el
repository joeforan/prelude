;; Avoid use of super-key generally conflicts
;; with OS, terminal
(setq prelude-super-keybindings nil)

;; Turn off menu in terminal mode
(menu-bar-mode -1)

;; Use zenburn
(setq prelude-theme 'zenburn)
